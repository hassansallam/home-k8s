# Kubernetes Event Exporter Helm values
# Logs cluster events to stdout for Promtail/log aggregator pickup

config:
  logFormat: json
  receivers:
    - name: dump
      stdout: {}
  route:
    routes:
      - match:
          - receiver: dump
