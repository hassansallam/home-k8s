admissionController:
  replicas: 1
  container:
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 512Mi

backgroundController:
  enabled: true

config:
  webhooks:
    - failurePolicy: Ignore
